export enum Path {
  HOME = "/",
  SEARCH = "/search",
  NOTIFICATION = "/notification",
  MESSAGE = "/message",
}

export const DEFAULT_AVATAR_URL = "/default_avatar.png";

export const COLORS = {
  darkGrey: "rgb(113, 118, 123)",
};
